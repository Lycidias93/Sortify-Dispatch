# Sortify Dispatch 4.8.11 - Contract smoke default-prefix hotfix

## What changed

- Fixed the safe **Contract smoke** action falsely reporting a failure when no optional custom park prefix is configured.
- Configured custom park prefixes are still checked when present.

## Update

Install the module update with a Magisk/KernelSU-compatible module manager and reboot once.
